============
Installation
============

At the command line::

    $ easy_install elasticsearch_backup

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv elasticsearch_backup
    $ pip install elasticsearch_backup